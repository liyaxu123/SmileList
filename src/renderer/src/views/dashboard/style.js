import { styled } from 'styled-components'

export const DashboardWrap = styled.div`
  width: calc(100vw - 60px);
  min-height: 100vh;
  position: relative;

  .left-pane {
    padding: 8px;
  }

  .right-pane {
    padding: 12px;
  }
`
